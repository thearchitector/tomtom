# tomtom

Guidelines and skills for agentic development.

```bash
$ sudo mount --bind --read-only ~/Codespace/tomtom/skills/ ~/.codex/skills
$ sudo mount --bind --read-only ~/Codespace/tomtom/agents/ ~/.codex/agents
```
